p#notice= notice
h1 Items
table.table
  thead
    tr
      th Title
      th Description
      th Date of adding
      th colspan="3" 
  tbody
    - @items.each do |item|
      tr
        td= item.title
        td= item.description
        td= item.created_at.strftime("%e %B %Y")
        td= link_to 'Show', item
        td= link_to 'Edit', edit_item_path(item)
        td= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } 
br/
= link_to 'New Item', new_item_path, class: 'btn btn-default'
